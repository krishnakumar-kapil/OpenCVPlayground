#include <iostream>
#include <string>
#include <opencv2/opencv.hpp>
using namespace cv;

int main(int argc, char** argv){
    //Mat orig_image;

    //orig_image = imread(argv[1]);
    //namedWindow("Original Image", WINDOW_AUTOSIZE);
    //imshow("Original Image", orig_image);
    cout << "asdf";
    //waitKey(0);
    return 0;
}
